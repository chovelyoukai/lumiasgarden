float remainder(float dividend, float divisor)
{
	local float quotient, remainder;
	
	quotient = floor(dividend / divisor);
	remainder = dividend - (quotient * divisor);

	return remainder;
}

void() drawHud =
{
	local float 
};
